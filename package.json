{
	"name": "rep-score-public-backend",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"description": "Open, Sybil-resistant reputation (Trust Rank) computed from the public Circles trust graph. No secrets, no analytics service.",
	"scripts": {
		"start": "node src/server.ts",
		"dev": "node --watch src/server.ts"
	},
	"engines": {
		"node": ">=22.6"
	}
}
