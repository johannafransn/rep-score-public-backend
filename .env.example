# All optional — sensible public defaults are baked in (see src/config.ts).
PORT=8787
# REFRESH_MS=600000
# DAMPING=0.85
# ITERATIONS=50
# PER_SOURCE_CAP=0.02
# Comma-separated anchor avatar addresses (Sybil-hardening). Empty = uniform seed.
# ANCHORS=0x...,0x...
